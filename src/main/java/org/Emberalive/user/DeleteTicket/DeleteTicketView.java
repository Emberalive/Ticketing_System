package org.Emberalive.user.DeleteTicket;

import javax.swing.*;

public class DeleteTicketView extends JFrame {
    public DeleteTicketView(int ticketID) {
        setTitle("Delete Ticket, ID: " + ticketID);
        setSize(600, 400);
    }
}
